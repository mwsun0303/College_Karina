# OracleDB connection settings
#spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
#spring.datasource.username=itbank08
#spring.datasource.password=itbank08
#spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.username=admin
spring.datasource.password=mariapass
spring.datasource.url=jdbc:mariadb://database-1.csnk7txwlwlg.ap-northeast-2.rds.amazonaws.com:3306/care


# mybatis framework setting
mybatis.mapper-locations=/mappers/*.xml

# JSP settings
spring.mvc.view.prefix=/WEB-INF/
spring.mvc.view.suffix=.jsp

# file settings
spring.servlet.multipart.max-file-size=10MB

# server port settings
server.port=80

server.tomcat.accesslog.enabled=true
server.tomcat.basedir=.
server.tomcat.accesslog.directory=logs
server.tomcat.accesslog.suffix=.log
server.tomcat.accesslog.prefix=access_log
server.tomcat.accesslog.file-date-format=.yyyy-MM-dd
server.tomcat.accesslog.pattern=%{yyyy-MM-dd HH:mm:ss}t %s %r %{User-Agent}i %{Referer}i %a %b %D


#CREATE DATABASE care;
#USE care;
#CREATE TABLE board_quiz(
#	no int,
#	title varchar(100),
#	content varchar(200),
#	id varchar(20),
#	write_date varchar(15),
#	hits int,
#	file_name varchar(255),
#	primary key(no)
#)DEFAULT CHARSET=UTF8;
#
#CREATE TABLE db_quiz(
# id varchar(20),
# pw VARCHAR(60),
# user_name varchar(21),
# address varchar(1000),
# mobile varchar(15),
# PRIMARY KEY(id)
#)DEFAULT CHARSET=UTF8;

# https://github.com/GoogleContainerTools/jib/tree/master/jib-maven-plugin
# compile jib:build -Djib.to.auth.username=kys8502 -Djib.to.auth.password=1234


